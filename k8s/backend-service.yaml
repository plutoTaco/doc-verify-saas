apiVersion: v1
kind: Service
metadata:
  name: doc-verify-backend-svc
spec:
  type: LoadBalancer
  selector:
    app: doc-verify-backend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080